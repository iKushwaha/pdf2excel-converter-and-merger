"""Web application package.

Exposes a session-scoped, server-side web app built on the same core
engine as the desktop app. Run with::

    python -m pdf2xlsx.web.server

Or via the console script ``pdf2excel-web``.
"""
